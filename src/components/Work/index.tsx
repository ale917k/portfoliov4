import React from "react";
import { Wrapper } from "./styles";

/**
 * Work Section.
 * @returns TO FILL OUT.
 */
const Work: React.FC = () => {
  return <Wrapper id="work">Work Section</Wrapper>;
};

export default Work;
