import React from "react";
import { Wrapper } from "./styles";

/**
 * Contact Section.
 * @returns TO FILL OUT.
 */
const Contact: React.FC = () => {
  return <Wrapper id="contact">Contact Section</Wrapper>;
};

export default Contact;
